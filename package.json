{
  "name": "@rodesp/www",
  "version": "0.1.0",
  "author": "Rodrigo Espinosa",
  "description": "My personal website",
  "homepage": "https://gitlab.com/rodesp/www#readme",
  "bugs": {
    "url": "https://gitlab.com/rodesp/www/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://gitlab.com/rodesp/www.git"
  },
  "license": "CC-BY-NC-ND-4.0",
  "engines": {
    "node": ">=10.0.0"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp src/client/index.html dist/ && cp -r src/client/css dist/ && cp -r src/client/resources dist/ && cp favicon.ico dist/ &&  webpack --mode development",
    "dev": "npm run build && npm start",
    "start": "node ./dist/server.js",
    "lint": "eslint **/*.js"
  },
  "dependencies": {
    "express": "^5.0.0-alpha.7"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "webpack": "^4.41.2",
    "webpack-node-externals": "^1.7.2"
  }
}
